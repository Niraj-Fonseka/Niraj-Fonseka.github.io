apiVersion: v1
kind: Service
metadata:
  labels:
    app: personal-site-prod
  name: personal-site-prod
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  selector:
    app: personal-site-prod